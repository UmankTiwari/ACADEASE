{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Chrome on Live Server",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:5000",
      "webRoot": "${workspaceFolder}/client/src"
    }
  ]
}
